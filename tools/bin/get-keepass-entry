#!/usr/bin/env fish

set username  $argv[1]
set url       $argv[2]

if [ (count $argv) -eq 2 ]
  printf "url=%s\nusername=%s\n" $url $username \
    | git-credential-keepassxc get \
    | sed -n 's/^password=//p'
else
  exit 1
end
